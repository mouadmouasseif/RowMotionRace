import { Medal, Trophy } from "lucide-react";
import { previewClubRankings } from "@/constants/interface-preview";

export const metadata = { title: "Classements" };
export default function RankingsPage() { return <div className="mx-auto max-w-4xl space-y-4"><div><p className="text-xs uppercase tracking-[.16em] text-race-primary">Championnat du Maroc 2026</p><h2 className="mt-1 text-xl font-semibold">Classement par clubs</h2></div><section className="race-card rounded-2xl p-4 sm:p-5"><div className="flex items-center justify-between border-b border-white/[0.06] pb-4"><p className="text-sm font-semibold">Points cumulés</p><Trophy className="size-5 text-race-warning" /></div><div className="divide-y divide-white/[0.06]">{previewClubRankings.map((item) => <div key={item.club} className="grid grid-cols-[36px_1fr_auto] items-center gap-3 py-4"><span className={`text-center text-xl font-black ${item.rank === 1 ? "text-race-warning" : item.rank === 2 ? "text-sky-300" : item.rank === 3 ? "text-orange-400" : "text-race-muted"}`}>{item.rank}</span><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg bg-race-primary/10"><Medal className="size-4 text-race-primary" /></span><p className="text-xs font-medium sm:text-sm">{item.club}</p></div><b className="text-sm tabular-nums">{item.points}</b></div>)}</div></section></div>; }
