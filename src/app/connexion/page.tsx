import { Suspense } from "react";
import Image from "next/image";
import Link from "next/link";
import { ArrowLeft, ShieldCheck } from "lucide-react";
import { BrandMark } from "@/components/shared/brand-mark";
import { LoginForm } from "@/features/authentication/login-form";

export default function LoginPage() { return <main className="grid min-h-screen lg:grid-cols-2"><section className="relative hidden overflow-hidden border-r border-white/10 bg-race-surface p-12 lg:flex lg:flex-col lg:justify-between"><div className="absolute -bottom-40 -right-40 size-[34rem] rounded-full bg-race-primary/10 blur-3xl" /><BrandMark /><div className="relative"><Image src="/brand/rowmotion-race-icon.png" width={112} height={112} alt="" className="size-24 rounded-[1.4rem] object-contain shadow-2xl" /><ShieldCheck className="mt-8 size-8 text-race-primary" /><h1 className="mt-6 max-w-xl text-5xl font-semibold leading-tight tracking-[-.04em]">Un seul compte pour toute la plateforme RowMotion.</h1><p className="mt-5 max-w-lg text-race-muted">Utilisez vos identifiants RowMotion AI existants. Aucun second compte n’est créé.</p></div><p className="relative text-xs uppercase tracking-[.18em] text-race-muted">Performance · Précision · Victoire</p></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-md"><Link href="/" className="mb-8 inline-flex items-center gap-2 text-sm text-race-muted"><ArrowLeft className="size-4" />Retour</Link><div className="mb-7 flex items-center gap-4 lg:hidden"><Image src="/brand/rowmotion-race-icon.png" width={64} height={64} alt="RowMotion Race" className="size-16 rounded-2xl object-contain" /><BrandMark /></div><p className="text-xs font-semibold uppercase tracking-[.18em] text-race-primary">Compte RowMotion AI</p><h2 className="mb-8 mt-3 text-3xl font-semibold">Connexion</h2><Suspense fallback={<div className="h-64 animate-pulse rounded-2xl bg-race-surface" />}><LoginForm /></Suspense><p className="mt-7 rounded-xl border border-race-warning/20 bg-race-warning/5 p-3 text-xs leading-5 text-race-muted">La configuration web est active. La création de session privée nécessite encore les trois paramètres Firebase Admin.</p></div></section></main>; }
